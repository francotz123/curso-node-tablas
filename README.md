# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     base para la multiplicación                [número] [requerido]
  -l, --listar   Si listar está en true, lista la tabla               [booleano]
  -h, --hasta    Numero que indica hasta donde debe ir el numero        [número]

```